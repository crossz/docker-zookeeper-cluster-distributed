version: '2'

services:
  zookeeper:
    build: .
    network_mode: "host"
    environment:
      - ENVI
      - MYID=1
      - SERVERS=localhost,localhost,localhost
    restart: always
    image: crossz/zookeeper-cluster-distributed
